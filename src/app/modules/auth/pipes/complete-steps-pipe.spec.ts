import { CompleteStepsPipe } from './complete-steps-pipe';

describe('CompleteStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new CompleteStepsPipe();
    expect(pipe).toBeTruthy();
  });
});
